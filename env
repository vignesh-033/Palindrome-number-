# Soloskillset Backend Environment Variables

# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
# Local MongoDB
MONGODB_URI=mongodb://localhost:27017/soloskillset

# Or use MongoDB Atlas (cloud) - Replace with your connection string
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/soloskillset?retryWrites=true&w=majority

# JWT Secret Key
JWT_SECRET=soloskillset-secret-key-change-this-in-production-2025

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5500,http://127.0.0.1:5500,http://localhost:3000
